local opts = {
  -- run = ":TSUpdate",
  -- sync_install = false,
	ensure_installed = { "go", },
	-- auto_install = true,
	highlight = {
	  enable = true,
	},
  indent = {
    enabled = true,
  },
  incremental_selection = {
    enable = true,
  },
}

return opts